{
  "name": "tdb-map",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "parcel build src/index.js --no-content-hash --no-source-maps --global tdbMap --out-dir tdb-map --public-url public/ --out-file tdb-map.js"
  },
  "staticFiles": {
    "staticPath": [
      {
        "staticPath": "public",
        "staticOutDir": "."
      }
    ]
  },
  "devDependencies": {
    "@babel/core": "^7.2.0",
    "parcel-bundler": "^1.6.1",
    "parcel-plugin-static-files-copy": "^2.6.0"
  },
  "dependencies": {
    "mapbox-gl": "^2.14.1"
  }
}
